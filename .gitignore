# Python cache and compiled files
__pycache__/
*.pyc

# The progress database is generated at runtime, not source code
progress.db

# Google API secrets — never commit these to a public repo
credentials.json
token.json

# Local session notes — not for the public repo
HANDOFF.md

# Virtual environment folders (if you create one later)
venv/
.venv/
env/

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
.DS_Store
